[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"
[project]
name = "driftguard"
version = "0.1.0"
description = "Cut a structured generation the instant it leaves the grammar."
requires-python = ">=3.9"
dependencies = []
[project.scripts]
driftguard = "driftguard.__main__:main"
