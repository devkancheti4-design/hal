[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"
[project]
name = "driftguard"
version = "0.2.0"
description = "Know when your agent starts degrading, before your users do."
requires-python = ">=3.9"
dependencies = []
[tool.setuptools]
packages = ["driftguard"]

[project.scripts]
driftguard = "driftguard.__main__:main"
